{{ include ('header.html.twig', {}) }}
    <link href="{{ asset('css/comun.css') }}" rel="stylesheet" type="text/css" />

<div class="container">
  <div class="mt-5 mb-5 d-flex justify-content-center">
    {{ form_start(form)}}<br>
    {{ form_widget(form) }}<br>
    {{ form_end(form) }}
  </div>
  {% if mensaje != '' %}
    <p class="text-danger fs-3">{{mensaje}}</p>
  {% endif %}
</div>
{{ include ('footer.html.twig', {}) }}