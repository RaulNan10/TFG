    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous">
    </script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"
        integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous">
    </script>

    <div class="container">
        
        {% if eventos %}
        {% for evento in eventos %}
        <div class="card border border-dark" style="width: 25rem;">
            <img src="/imgs/event/{{evento.image}}" class="card-img-top" alt="...">
            <div class="card-body">
                <p class="card-title fw-bold fs-2 text-uppercase">{{evento.title}}</p>
                <hr class="border border-primary mt-0">
                <p class="card-text lh-lg fs-5 mt-0">{{evento.description}}</p>
            </div>
            <div class="card-footer border border-top">
                <p class="fs-4 text">Valoraciones
                    <svg type="button" data-bs-toggle="modal" data-bs-target="#exampleModal"
                        xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                        class="bi bi-plus-circle" viewBox="0 0 16 16">
                        <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z" />
                        <path
                            d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z" />
                    </svg></p>

            </div>
        {% if comentarios %}
            <ul class="list-group list-group-flush">
        {% for comentario in comentarios %}
                <li class="list-group-item"><span class="text-muted"><img class="rounded-circle" height="20" width="20" src="/imgs/user/{{comentario.user.image}}"/>{{comentario.user.name}}.</span>  {{comentario.description}}</li>
                
            
        {% endfor %}
            </ul>
            <div class="card-body bg-light">
                <p class="text-primary" style="--bs-text-opacity: .5;">Evento organizado por <span class="text-uppercase fw-bold text-primary">{{evento.user.name}}</span></p>
            </div>
        {% endif %}
            
        </div>
        {# MODAL #}
        <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="exampleModalLabel">Tu comentario</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        {{ form_start(form) }} {{ form_widget(form) }} {{ form_end(form) }}
                    </div>
                    <div class="modal-footer">
                        
                    </div>
                </div>
            </div>
        </div>
        {# MODAL #}

        {% endfor %}
        {% endif %}
        {% if correcto == 'si' %}
        <div class="alert alert-success mt-3" role="alert">
            Tu comentario ha sido realizado satisfactoriamente— verEventos
        </div>
        {% endif %}
    </div>